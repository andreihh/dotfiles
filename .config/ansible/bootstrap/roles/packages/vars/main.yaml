---
# file: roles/packages/vars/main.yaml

packages_list:
  common:
    # Also available as brew formulae.
    core:
      - git
      - stow
      - curl
      - wget
      - zip
      - gzip
      - unzip
      - tar
      - gnupg
      - tmux
      - urlscan
      - vim
      - fzf
      - fd-find
      - ripgrep
      - bat
      - tree
      - calc
      - dos2unix
      - make
      - automake
      - cmake
      - gcc
      - pre-commit
      - htop
      - cmus
      - cava
    # Also available as brew casks.
    media:
      - keepassxc
  apt:
    - gnome-shell-extension-appindicator
    - lm-sensors
    - build-essential
    - linux-tools-common
    - linux-tools-generic
    - gtk2-engines-murrine
  dnf:
    - gnome-shell-extension-appindicator
    - lm_sensors
    - gcc-c++
    - gtk-murrine-engine
  brew:
    formulae:
      - coreutils
      - lm-sensors
    casks:
      - alt-tab

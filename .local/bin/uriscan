#!/bin/sh
#
# Lists URIs found in the standard input.

# Exit if any command fails.
set -e

[ $# -gt 0 ] && echo "Usage: $0" && exit 1

readonly URI_PATTERN='(https?|file)://[-a-zA-Z0-9@:%_+.~#?&/=]+[-a-zA-Z0-9@%_+.~#?&/=!]+'

grep -Eo "${URI_PATTERN}" -

# sleep.conf: device sleep config to be placed in `/etc/systemd/sleep.conf.d`.
#
# Allow suspend, but disable all forms of hibernation.

[Sleep]
AllowSuspend=yes
AllowHibernation=no
AllowSuspendThenHibernate=no
AllowHybridSleep=no
